require File.dirname(__FILE__) + '/../helpers/spec_helper.rb'

describe ProjectFile do
  before :each do
    TempHelper::cleanup
  end
  it 'should be able to parse the xml and locate the project type guids' do 
  end
end
